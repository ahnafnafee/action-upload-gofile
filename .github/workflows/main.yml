name: "Main"

on:
    push:
        branches:
            - master

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2

            - name: Upload File
              uses: ./
              id: gofile
              with:
                  token: ${{ secrets.GOFILE_TOKEN }}
                  file: ./example.apk

            - name: Output
              run: echo "Gofile URL = ${{ steps.gofile.outputs['url'] }}"
